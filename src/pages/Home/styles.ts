import styled from 'styled-components'

export const ButtonChangeUser = styled.button`
  padding: 1.5rem;
  font-size: 1.75rem;

  background-color: #61dafb;

  border: none;
  border-radius: 2px;

  &:hover {
    filter: brightness(0.9);
    cursor: pointer;
  }
`
